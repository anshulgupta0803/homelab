version: '3'

services:
    nginx:
        container_name: nginx
        image: nginx:latest
        volumes:
         - ./nginx.conf:/etc/nginx/nginx.conf:ro
        ports:
         - "80:80"
        networks:
            - phantom-net

networks:
    phantom-net:
        external: true
